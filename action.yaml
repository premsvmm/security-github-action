name: security-github-action
description: "Security action to perfrom on CI pipeline"
inputs:
  github-token:
    required: true
    description: The GitHub token used to create an authenticated client.  Defaults to github provided token.
    default: ${{ secrets.GIT_TOKEN }}
runs:
  using: 'docker'
  image: 'Dockerfile'